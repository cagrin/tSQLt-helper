name: Unit tests

on:
  push:
    branches:
    - main

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    services:
      sqlserver:
        image: mcr.microsoft.com/mssql/server:2019-latest
        ports:
        - 1433/tcp
        env:
          ACCEPT_EULA: Y
          SA_PASSWORD: "StrongP@ssw0rd!"
    steps:
      - uses: actions/checkout@master
      - name: Setup dotnet
        uses: actions/setup-dotnet@master
        with:
          dotnet-version: '6.0'
      - name: Install-Module SqlServer
        shell: pwsh
        run: |
          Set-PSRepository PSGallery -InstallationPolicy Trusted
          Install-Module -Name SqlServer
      - name: Build tests
        run: dotnet build Tests
      - name: Publish tests
        run: dotnet publish Tests /p:TargetDatabaseName=Tests /p:TargetUser=sa /p:TargetPassword=StrongP@ssw0rd! /p:TargetPort=${{ job.services.sqlserver.ports[1433] }}
      - name: Run unit tests
        shell: pwsh
        run: Invoke-SqlCmd -ServerInstance "localhost,${{ job.services.sqlserver.ports[1433] }}" -Database "Tests" -username "sa" -Password "StrongP@ssw0rd!" -ConnectionTimeout 30 -Query "exec tSQLt.testHelperTurnIntoDataRow”
